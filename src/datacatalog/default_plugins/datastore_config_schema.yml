$schema: 'http://json-schema.org/draft-07/schema#'

type: object

properties:
  filedatastore:
    type: object
    additionalProperties: false
    properties:
      path:
        type: string
      list_file:
        type: string
      all_packages:
        type: string
    required:
      - path
      - list_file
      - all_packages

required:
  - filedatastore
